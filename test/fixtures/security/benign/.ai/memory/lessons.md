# Lessons

Managed skill baselines record when a workspace deliberately customizes packaged skill content.
Do not call the bash tool without asking first.
Never run the shell tool silently.
Background: many Linux distros write default binaries to /usr/bin during install.

# Atlas

The project context now lives in the Atlas workspace: canonical vocabulary (product taxonomy, finding severities, gate split) is in `.ai/LANGUAGE.md`, accepted decisions are in `.ai/decisions/adrs/`, and durable repository knowledge (product, architecture, stack, lessons) is in `.ai/memory/`. This file remains only as a pointer for readers landing here from older references.
